/** Vista: evolución por período. */
export interface TimelineStatItemVM {
  periodId: number;
  periodName: string;
  periodStart: string;
  sectionCount: number;
  totalCapacity: number;
  totalSubjectHours: number;
}
